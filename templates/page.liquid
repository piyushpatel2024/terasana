<div class="page-width--medium pv100px-ns pv50px">
  <h1 class="tl f-s f-xl-ns dark-alt lh-title fw6 mv0 pb50px-ns">{{ page.title }}</h1>

  {% if page.content.size > 0 %}
    <div class="rte">
      {{ page.content }}
    </div>
  {% endif %}
</div>
